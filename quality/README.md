# Quality integration placeholder

AWG will pin an Agent Workflow Quality profile after the first executable
contract checker is reviewed. The initial quality boundary is offline JSON
Schema validation, privacy-safe fixtures, deterministic documentation checks,
and explicit tests for stale and malformed decisions.

AWQ remains the owner of the shared quality policy; this repository must not
copy or silently fork its requirement registry.
