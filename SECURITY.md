# Security policy

Do not report vulnerabilities in public decision packets, task notes, or
issues when they contain secrets, private prompts, host identifiers, or
unredacted evidence. Remove sensitive material locally and use the repository
owner's private reporting channel.

AWG records are decision provenance, not an authorization to perform unsafe
actions. Integrations must apply their own least-privilege, approval, and
rollback controls.
